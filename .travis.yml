language: ruby
rvm:
  - 2.0.0
env:
  - ISOLATED=true
  - DB=postgresql

before_script:
  - gem update --system
  - gem --version
  - cp config/database.travis.yml config/database.yml
  - psql -c 'create database prometheus_test;' -U postgres
