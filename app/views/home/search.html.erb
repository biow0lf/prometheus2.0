<% srpm_count @package_counter %>
<% title "Sisyphus 2.0 - Search" %>

<div class="header" style="letter-spacing:1px;">Поиск по сайту</div>
<p class="break">
<form name="findonsite" action="/find.shtml" method=GET>
<input type="text" name="request" value="<%=h params[:request] %>" size="20">
<input type="submit" class="top-go" id="subOn" value="искать" onmouseover="m_on('subOn')"  onmouseout="m_off('subOn')">
</form>
<br /><br />
По запросу &quot;<b><%=h params[:request] %></b>&quot; было найдено <b><%=h @srpms_count %></b> пакетов:

<p class="break"><br />
<table id="bugs" cellspacing="0" cellpadding="3">
<tr>
<th>пакет</th>
<th>текущая<br />версия</th>
<th>сборщик</th>
<th>о пакете</th>
</tr>

<% @srpms.each do |s| %>
<tr>
<td class="strange">
  <%= link_to h(s.name), '/' + h(params[:locale]) + '/srpm/Sisyphus/' + h(s.name), :class => 'news' %>
</td>
<td><%=h s.version %>-<%=h s.release %></td>
<td>
<% if s.packager.team == true %>
  <%= link_to h(s.packager.name), '/' + h(params[:locale]) + '/team/' + h(s.packager.login)[1..-1], :class => 'news' %>
<% else %>
  <%= link_to h(s.packager.name), '/' + h(params[:locale]) + '/packager/' + h(s.packager.login), :class => 'news' %>
<% end %>
</td>
<td><%=h s.summary %></td>
</tr>
<% end %>

</table>

