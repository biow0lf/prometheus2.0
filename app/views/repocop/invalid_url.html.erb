<% @srpms.each do |srpm| -%>
<%= srpm.name %>-<%= srpm.version %>-<%= srpm.release %>	experimental	invalid-url	Package has invalid Url:. It looks like a  protocol part (http:,ftp:,etc.) is missing.;
<% end -%>
